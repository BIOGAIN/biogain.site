/**
 * Global toggle for scroll-triggered entry animations.
 *
 * Controls every element using `data-animate` across the site
 * (Hero, Features, Stats, Testimonials, news cards, etc.).
 *
 * Set to `false` to make all those elements appear immediately
 * with no fade/slide/scale on page load.
 *
 * Note: continuous decorative animations (Hero halo pulse,
 * HeroGradient floating orbs) are scoped to their components
 * and not controlled by this flag.
 */
export const ANIMATIONS_ENABLED = false;
